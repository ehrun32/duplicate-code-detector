// file3.js

// Same as add in file1.js
function add(a, b) {
  return a + b;
}

// Slightly different greet logic
function greet(name) {
  console.log("Hi there, " + name);
}

function subtract(a, b) {
  return a - b;
}
